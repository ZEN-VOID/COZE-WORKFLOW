# Projects

该目录用于存放已经开发完成或正在持续迭代的具体项目。

## 目录约定

- 推荐结构：`projects/<project_name>/`
- 支持多个项目并存
- 每个项目应尽量自包含，至少具备自己的说明文档、配置与运行方式

## 协作约定

- 当明确指定项目名称时，开发和修改应限制在对应项目子目录内
- 若变更属于跨项目共性能力，应优先回流到模板层或仓库级规范
- 若从模板创建新项目，建议以 `examples/` 为起点进行复制或抽取
